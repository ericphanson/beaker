data
.venv
*.pt
runs
crops
models
__pycache__
onnxruntime-libs

# Rust build artifacts
beaker/target/
beaker/Cargo.lock
target/
example-raw.rw2
beaker/*-notes.md

# Beaker output files - test artifacts that should not be committed
*.beaker.toml
*.beaker.json

# Stamp files - test artifacts that should not be committed
*.stamp

# Image files in beaker directory - examples should be at top-level
beaker/*.jpg
beaker/*.jpeg
beaker/*.png
beaker/*.bmp
beaker/*.gif
beaker/*.webp
beaker/*.tiff
beaker/*.tif
*.png
*_bounding-box.jpg
*_cutout.*

# Exception: example_crop.jpg is shown in README.md and should be committed
!example_crop.jpg
