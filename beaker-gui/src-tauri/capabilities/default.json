{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:image:default",
    "core:image:allow-from-tauri-src",
    "opener:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
